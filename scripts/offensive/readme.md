# Compatibility Path

This folder is a compatibility shim.

Use the active location instead:

- `../off/readme.md`
- `../off/SafeRedTeamSuite.ps1`

The suite dispatcher (`Invoke-Tcpent.ps1 -Action off`) already loads from `scripts/off`.
